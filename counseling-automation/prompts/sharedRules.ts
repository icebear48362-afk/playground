/**
 * 모든 분석 프롬프트가 공통으로 지켜야 하는 추출 규칙.
 * 규칙을 바꾸고 싶으면 이 파일만 수정하면 모든 프롬프트에 반영된다.
 */
export const SHARED_EXTRACTION_RULES = `
- 없는 내용은 절대 추측하지 않는다.
- 고객이 직접 말한 내용을 우선으로 작성한다.
- 개인정보는 최소한만 추출한다.
- 상담과 무관한 사적인 대화는 제외한다.
- 상담 핵심만 요약한다.
- 각 항목은 최대 2줄 정도로 작성한다.
- 상담사가 설명한 내용은 지정된 항목에만 작성하고 다른 항목에 섞지 않는다.
`.trim();
